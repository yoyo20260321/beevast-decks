# BeeVast 官网 PC UI 精修设计

日期：2026-09-21  
范围：`beevast-home-v4/index.html` PC 端视觉精修  
基准视口：1440 × 900；交付整页长图  
状态：用户已确认设计方向 A「全站克制精修」

## 1. 目标

在不改变现有业务信息架构、不新增外部素材的前提下，降低通用 SaaS 与 AI 模板感，使官网呈现更稳重、克制、可信的企业级品牌质感。

完成标准：

- 首屏主次关系明确，标题、说明与转化按钮形成单一视觉路径。
- 字体、间距、圆角、边框、阴影和图标尺度统一。
- “智能售后客服”典型场景与“真实案例”形成明显层级差。
- 关键业务信息与证据口径不被视觉装饰淹没。
- PC 页面通过现有静态校验，1440px 全页截图无溢出、断层或隐藏内容。

可量化的视觉门槛：

- PC 主标题内容宽度不超过 760px；正文行长不超过 68 个中文字符的视觉宽度。
- 一级 section 的桌面上下留白统一在 88–104px；同类标题区到主体内容的距离统一在 32–40px。
- 主卡片圆角统一为 12–16px，边框为 1px；常态阴影只保留一层低对比阴影，悬停位移不超过 2px。
- 品牌金只用于主行动、关键标签、流程节点和核心数字；同一首屏内高饱和金色焦点不超过 3 个。
- “智能售后客服”与真实案例不得使用完全相同的主体布局：前者展示流程链路，后者保留证据型三栏案例结构。

## 2. 视觉方向

沿用深海军蓝、暖白与品牌蜂蜜金，但减少渐变、发光和高频装饰。白色区域依靠排版、边框与留白建立质感；深色区域依靠层次化背景与低对比纹理建立空间，不使用浮夸光效。

字体继续使用现有系统字体栈，避免引入第三方字体请求。标题减少过粗字重，正文提高行高与对比度；小字号仅用于辅助说明和数据口径。

放弃：大规模品牌换色、外部插画、新字体和全新页面结构。原因是本轮目标为低风险精修，而非重新设计。

## 3. 页面级调整

### 3.1 首屏

- 收紧主标题最大宽度，建立更清晰的两到三行节奏。
- 现有首屏没有独立说明段；保持可见文案不变，优化“标题 → CTA → 信任证据带”的间距与层级。
- 主按钮保持品牌金，次按钮降低填充与边框对比。
- 产品展示框减少发光和悬浮感，强调真实工作台质感。
- 信任信息通过样式降级与间距分离，保持现有 DOM 顺序和文案不变。

### 3.2 通用区块

- 统一 section 上下留白和标题区宽度。
- 卡片统一圆角、边框、阴影和内边距，减少每张卡都强调的竞争感。
- 图标统一容器尺寸、描边粗细和色彩饱和度。
- 关键数字放大，辅助说明降级，避免卡片内部权重平均。

### 3.3 典型场景

“智能售后客服”保留独立展示，不进入真实案例轮播。允许在该现有 section 内调整标记和视觉布局，但不改变 section 的页面顺序。其主体采用三段横向业务链路：

1. 客户询问与运单识别；
2. 节点聚合、异常判断与回复；
3. 建单、分派、催办和人工审批。

业务链路是该模块的主要视觉，不再沿用普通案例的 Before/After 表格感。Pilot 指标作为右侧或底部辅助证据区，明确首次响应、一次解决率、新人上手周期、异常闭环时长四项口径。

### 3.4 真实案例

- 保留三个有证据支撑的案例轮播。
- 统一标题、标签、指标和脚注格式。
- 指标区不使用强渐变大字，改为更克制的数据面板。
- 轮播导航降低装饰感，同时保持键盘、鼠标和触控可用。

## 4. 动效与交互

- 保留轻微淡入和卡片悬停反馈。
- 动效位移与阴影变化幅度降低。
- 遵守 `prefers-reduced-motion`，不新增持续漂浮、发光或自动吸睛动画。
- 不改变导航、表单、轮播及移动端既有交互逻辑。

## 5. 响应式边界

本轮以 PC 为主，但不得破坏既有移动端样式。新增或调整的规则优先限定在桌面断点；通用 token 变化需在 860px、640px 和 420px 断点复核。

响应式与交互用例：

- 1440×900：导航、首屏、典型场景三段链路、案例三栏和页脚均无横向溢出。
- 860×900：典型场景与案例由多栏转为单栏，文字与箭头不重叠。
- 640×900、420×860：导航切换按钮可用，正文不小于既有移动端下限，CTA 不超出视口。
- 键盘：导航、案例箭头、轮播圆点和表单控件均可聚焦；Enter/Space 可触发按钮。
- 鼠标/触控：案例轮播可通过箭头与横向滑动切换；预约表单可打开和关闭。
- reduced-motion：内容直接可见，不依赖滚动动画出现。

## 6. 验证与交付

- 运行 `node scripts/verify-home-v4.mjs` 与 `git diff --check`。
- 用 Chrome/Playwright 以 1440 × 900 视口、`fullPage` 模式截取完整页面；截图前启用 reduced-motion 或滚动触发全部 reveal 元素。
- 精修前长图固定为 `output/beevast-home-pc-before-2026-09-21.png`，精修后长图固定为 `output/beevast-home-pc-after-2026-09-21.png`；两张图片必须为 1440px 宽，并记录像素高度。
- 按上一节的四个视口逐项检查布局；按键盘、鼠标/触控、表单、轮播和 reduced-motion 用例复核交互。
- 对精修前后长图进行视觉复核，重点检查首屏层级、section 节奏、典型场景/真实案例分层和页脚收束。
- 体积门槛沿用 `scripts/verify-home-v4.mjs`：HTML gzip ≤ 24,000B、关键资源 ≤ 95,000B、相对基线缩减 ≥ 20%、第三方字体请求为 0。
- 不新增外部图片、字体、脚本、样式依赖或网络请求；通过源文件 diff 与资源引用扫描确认。
- 开发预览经分支、PR 合入 `main` 后由 GitHub Pages 发布到 `https://yoyo20260321.github.io/beevast-decks/beevast-home-v4/`；以 HTTP 200 和页面出现“智能售后客服”为生效证据。
- 不执行 `deploy.sh prod`，不连接阿里云服务器；正式站发布需用户再次确认。

允许变更/生成的文件：

- 修改：`beevast-home-v4/index.html`
- 设计记录：本文件
- 本地交付物：上述两张 PNG 长图（不提交仓库）

回滚：开发预览若验证失败，对合入 `main` 的精修 PR merge commit 执行 `git revert`，重新等待 GitHub Pages 生效；正式站未变更，无生产回滚动作。

## 7. 风险

- 单文件 HTML 已接近 gzip 体积门槛，新增 CSS 必须通过精简或替换既有规则控制体积。
- 全站 token 调整可能影响移动端，需保持断点验证。
- 现有 reveal 动效可能造成自动长图隐藏未进入视口的内容；截图时使用 reduced-motion 或逐段滚动后再捕获。
