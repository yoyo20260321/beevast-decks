# 智能客服工作台「石墨墨玉」视觉升级规格

日期：2026-09-24  
目标页面：`demos/scenario-1-cs.html`  
已确认方向：A · 石墨墨玉

## 1. 目标与边界

在不改变现有业务结构、功能与示意数据的前提下，将页面从“通用浅色 SaaS 后台”升级为具有品牌记忆、材质层次和专业可信感的高端企业工作台。保留会话、语言切换、发送、转人工、建单、数据分析及响应式交互。

本次不增加新业务功能、不接入外部资源、不修改 GitHub Pages 路径。

本规格是既有 [`2026-09-24-scenario-1-cs-product-ui-design.md`](./2026-09-24-scenario-1-cs-product-ui-design.md) 的视觉增补并具有视觉条款优先级：既有规格第 2 节“视觉系统”中关于浅色中性底、青绿色品牌色、边框分区的描述由本规格替代；既有规格的功能范围、信息架构、状态模型、交互、响应式、可访问性、实现约束、验证与发布要求继续生效。

## 2. 视觉系统

- 主画布采用偏暖瓷白，消除大面积冷灰造成的廉价感。
- 导航采用深石墨色，内容面使用暖白与浅石色，通过色温差而非重边框分区。
- 墨玉绿是唯一核心交互色，用于当前导航、主按钮、选中态、焦点态与关键数据。
- 香槟金仅用于 BeeVast 品牌标识、少量关键标签或强调线，禁止大面积铺色。
- 风险、成功、等待仍使用红、绿、琥珀语义色；颜色必须与文字或图标共同表达状态。
- 字体继续使用系统字体栈；加强数字与标题字重，正文保持舒适、克制。
- 卡片减少可见边框，主要依靠留白、细分割线、轻微色面和低扩散阴影建立层次。
- 圆角统一收紧：主面板 10–12px，控件 6–8px，胶囊只用于短标签。

### 2.1 CSS Token

| Token | 色值 | 用途 |
|---|---:|---|
| `--canvas` | `#EEEDE7` | 页面主画布、消息区背景 |
| `--surface` | `#FAF9F5` | 顶栏、卡片、编辑器与弹层 |
| `--surface-muted` | `#F2F0E9` | 次级分组与 hover 色面 |
| `--nav` | `#16211F` | 主导航背景 |
| `--nav-hover` | `#22322E` | 导航 hover/当前项底色 |
| `--jade` | `#1D5A50` | 主按钮、选中态、交互强调 |
| `--jade-hover` | `#174C44` | 主按钮 hover |
| `--jade-soft` | `#DDE8E3` | 当前会话、客户消息浅色面 |
| `--champagne` | `#D5B977` | 仅品牌标识和短强调线 |
| `--text` | `#18211F` | 正文和标题 |
| `--muted` | `#66716D` | 次要正文 |
| `--nav-text` | `#BFC8C4` | 导航默认文字 |
| `--divider` | `#D8D5CA` | 分割线与必要控件边界 |
| `--control-border` | `#7D8581` | 输入框、按钮等必要控件边界 |
| `--chart-blue` | `#697B8A` | 图表辅助序列一 |
| `--chart-warm` | `#94704A` | 图表辅助序列二 |
| `--success` | `#2F6B4F` | 已完成、安全、成功 |
| `--danger` | `#A43D34` | 风险、错误、升级 |
| `--warning` | `#8A5C18` | 待确认、等待 |
| `--focus-light` | `#1D5A50` | 浅色面 2px 键盘焦点实线 |
| `--focus-dark` | `#D5B977` | 深色导航 2px 键盘焦点实线 |
| `--shadow` | `0 16px 40px rgba(22,33,31,.10)` | 弹层和必要浮层 |

关键对比组合按实现前计算值验收：`--text`/`--surface` 15.62:1，`--muted`/`--surface` 4.81:1，`--nav-text`/`--nav` 9.65:1，`--champagne`/`--nav` 8.68:1，白色/`--jade` 7.98:1，`--control-border`/`--surface` 3.60:1，`--chart-blue`/`--surface` 4.15:1，`--chart-warm`/`--surface` 4.26:1，`--focus-light`/`--surface` 7.58:1，`--focus-dark`/`--nav` 8.68:1。香槟金禁止作为浅色背景上的正文颜色。

焦点样式使用“2px 实色轮廓 + 3px 半透明外晕”：浅色面使用 `--focus-light`，深色导航使用 `--focus-dark`。`--divider` 只作结构分隔，不得承担必要控件边界；输入框、次要按钮和其他需要可感知边界的控件使用 `--control-border`。

交互色与状态色严格分工：墨玉色只表达主操作、选中和键盘焦点；完成/安全使用独立 `--success`，风险使用 `--danger`，等待使用 `--warning`，不得用墨玉色冒充业务完成状态。

## 3. 核心区域处理

1. 顶栏：品牌区域加入克制的香槟金标识；语言、在线状态和建单按钮重新分级，避免所有控件同权。
2. 左侧导航：石墨底、低对比默认项；当前项采用深墨玉色面与金色细标记，替代普通灰色高亮。
3. 会话列表：默认项弱化边框；当前会话使用暖墨玉浅色面与明确的左侧状态标识。
4. 消息区：暖石色背景承托暖白消息卡；客户消息使用浅墨玉色面，系统事件保留审计时间线形态。
5. 证据栏：从三张同权卡片调整为一条紧凑证据带，风险项以文字和细色标强调，不制造“AI 卡片墙”。
6. AI 草稿：作为主操作前的审核面板，使用暖白实体面、墨玉左标、来源和置信度分层展示；不使用发光、虚线或渐变。
7. 右侧上下文：使用分组标题、数据行和少量浅色面，降低嵌套卡片感；关键数字加强字重。
8. 数据分析：继承同一色板；主序列墨玉绿，辅助序列低饱和蓝灰，强调数据可读性而非装饰。
9. 弹层与提示：暖白面板、石墨标题、柔和遮罩；交互焦点使用墨玉色，完成反馈使用独立成功色。

## 4. 交互与响应式

- 保持现有发送、转人工、建单、会话切换、分析筛选等行为不变。
- hover 仅做轻微色面与边框变化，不使用漂浮或缩放式“炫技”动画。
- 保持清晰可见的键盘焦点、禁用态和异步反馈；遵循 `prefers-reduced-motion`。
- 1440×900、1024×768、390×844 三档不得出现遮挡、横向溢出或主要操作不可达。
- 小屏仍以会话和编辑器为主，上下文使用抽屉；视觉色板与层级保持一致。

## 5. 验收标准

- 页面不再出现大面积冷灰与通用青绿组合，四个核心色角色明确：石墨、瓷白、墨玉、香槟金。
- 品牌金只作点睛，不能与主操作争抢注意力。
- 首屏主视觉顺序清晰：当前会话 → 客户问题 → 证据/风险 → 草稿 → 审核发送。
- 正文对比度至少 4.5:1，大文本和非文本控件至少 3:1；逐项覆盖导航默认项、香槟金标识、禁用态、图表辅助序列、输入边界和焦点环。
- 三档截图检查通过；核心交互回归通过；控制台无阻断错误。
- 页内版本标识更新，并在合并 `main` 后验证公网 URL 返回 200、展示新视觉版本。
- 实现中所有核心色均引用约定 token；主按钮只使用 `--jade`，`--champagne` 不得用于按钮、大片背景或业务状态。
- 三档视口均按“当前会话 → 客户问题 → 证据/风险 → 草稿 → 审核发送”检查首屏层级；桌面端五级均应在首屏可见，移动端允许纵向滚动但顺序不得改变。
- 使用对比度脚本检查本节规定的关键前景—背景组合，并把计算结果纳入验收记录。

## 6. 明确放弃

- 放弃纯黑、霓虹、光晕和高饱和渐变，避免回到“AI 驾驶舱”。
- 放弃全局金色装饰，换取克制与可信。
- 放弃通过大量边框制造层级，改用色温、留白和排版。
- 放弃改动业务架构和数据结构，确保本轮升级聚焦视觉品质且易于回退。
